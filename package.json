{
  "name": "project_graphQL",
  "version": "1.0.0",
  "main": "src/server.ts",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "yarn -s clean && yarn -s generate && tsc",
    "generate": "yarn -s generate:prisma && yarn -s genearate:nexus",
    "generate:prisma": "prisma generate",
    "generate:nexus": "NODE_ENV=development ts-node --transpile-only src/schema"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@prisma/cli": "^2.11",
    "@types/node": "^14.14.6",
    "ts-node": "^9.0.0",
    "ts-node-dev": "^1.0.0",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "@nexus/schema": "^0.18.0",
    "@prisma/client": "^2.11.0",
    "apollo-server": "^2.19.0",
    "graphql": "^15.4.0",
    "nexus-plugin-prisma": "^0.23.1"
  }
}
