{
	"name": "harvestrtest",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"nodedev": " ts-node src/index.ts ",
		"dev:nexus": "ts-node-dev --transpile-only --no-notify src/api/app.ts",
		"build": "tsc",
		"generate": "yarn -s generate:prisma && yarn -s generate:nexus",
		"generate:prisma": "prisma generate",
		"generate:nexus": "ts-node --transpile-only src/api/schema",
		"test:nexus": "npm run generate:nexus && jest"
	},
	"jest": {
		"preset": "ts-jest",
		"globals": {
			"ts-jest": {
				"diagnostics": {
					"warnOnly": true
				}
			}
		},
		"testEnvironment": "node"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@nexus/schema": "^0.19.2",
		"@prisma/cli": "^2.13.0",
		"@prisma/client": "^2.13.0",
		"@prisma/codemods": "^0.6.1",
		"@types/jest": "^26.0.17",
		"@types/node": "^14.14.10",
		"apollo-server": "^2.19.0",
		"get-port": "^5.1.1",
		"graphql": "^15.4.0",
		"graphql-request": "^3.3.0",
		"jest": "^26.6.3",
		"nexus-plugin-prisma": "^0.25.0",
		"nodemon": "^2.0.6",
		"prettier": "^2.2.1",
		"ts-jest": "^26.4.4",
		"ts-node": "^9.1.1",
		"ts-node-dev": "^1.0.0",
		"typescript": "^4.1.2"
	}
}
