/// <reference types="node" />
import * as http from 'http';
export declare class H1Client {
    agent: http.Agent;
    closed: boolean;
    constructor();
    status(port: number): Promise<unknown>;
    request(port: number, body: string): Promise<any>;
    close(): void;
}
